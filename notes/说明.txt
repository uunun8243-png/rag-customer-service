本目录（notes/）是 RAG 问答的知识库目录。

只有这里的 *.md 文件会被索引进库（默认配置，可用环境变量 RAG_NOTES_DIR 指向其他目录）。

- 递归扫描时会自动跳过 .venv / .git / .rag_index / __pycache__ 等目录。
- 文件名含 dirty_raw 的 .md 会被排除（清洗前的中间产物不入库）。
- 文件内容变更后，需要重启服务或加 --rebuild 重新建索引（增量更新待实现）。

当前入库文件：
  clean.md —— XX商城售后规则（清洗后的知识库）
